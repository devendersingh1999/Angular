import { HighLightTodoDirective } from './high-light-todo.directive';

describe('HighLightTodoDirective', () => {
  it('should create an instance', () => {
    const directive = new HighLightTodoDirective();
    expect(directive).toBeTruthy();
  });
});
