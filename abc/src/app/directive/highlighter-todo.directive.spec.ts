import { HighlighterTodoDirective } from './highlighter-todo.directive';

describe('HighlighterTodoDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlighterTodoDirective();
    expect(directive).toBeTruthy();
  });
});
